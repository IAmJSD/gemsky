<%= render FormHeaderComponent.new(
    title: 'Change Password',
    description: 'Please enter your email address below to change your password.',
) %>

<%= render DesktopSmallerViewComponent.new do %>
    <%= form_with method: :post, data: {turbo: false} do %>
        <% if @maybe_successful %>
            <%= render NotificationComponent.new(
                type: 'success',
                body: ['If the e-mail address is attached to a Clearsky user, a password reset was sent.'],
            ) %>
        <% end %>

        <%= render InputComponent.new(
            name: 'email',
            label: 'E-mail Address',
            type: 'email',
            value: params[:email],
        ) %>

        <div class="mt-5 text-center">
            <%= render ButtonComponent.new(
                name: 'submit',
                label: 'Submit',
                type: 'submit',
                icon: 'envelope',
            ) %>
        </div>
    <% end %>
<% end %>
